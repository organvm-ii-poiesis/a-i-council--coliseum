# Backend runtime dependencies for the current in-memory MVP.
# Test/dev-only tools live in requirements-test.txt, which imports this file.
# Deferred integrations (full blockchain adapters, external ML stacks, etc.)
# stay on the roadmap until production contracts are finalized.

fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-dateutil==2.9.0.post0
httpx==0.28.1
sqlalchemy==2.0.52
asyncpg==0.31.0
greenlet==3.5.5
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4
litellm==1.96.2
solana==0.40.1
# anchorpy intentionally omitted: unused in the application codebase and
# incompatible with the upgraded solana/httpx stack used to clear pip-audit findings.
pgvector==0.5.0
web3>=7.16.0
email-validator
python-multipart
aiosqlite
redis>=8.1.0
